//import { useHistory } from "react-router-dom";

 /* function Redirection() {
    const history = useHistory();
    const doRedirection = () => {
        let url = "newURL";
        history.push(url);
    }
    return (
        <a name="Boutton" id="bouton-redir" class="btn btn-primary" onClick={doRedirection} role="button"></a>
    )
}

export default Redirection; */